profile: ".zsh_profile"
sources_codes: "${HOME}/.sources_codes"

c_cpp:
  enable: true
  development: true

rust:
  enable: true
  cargo_home: "${HOME}/.local/cargo"
  rustup_home: "${HOME}/.local/rustup"

python:
  enable: true
  projectManagers: "uv"

utilities:
  tmux: true
  zsh:
    enable: true
  neovim:
    enable: true
    version: "0.12.0"

gitconfig:
  email: "serebraykov.vs@gmail.com"
  name: "Omnivanitate"
  credential:
    helper: "cache"

special: true
